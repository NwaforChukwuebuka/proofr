# PROOFR — Handoff Log

Cross-milestone continuity: what shipped, what's mocked and why, env vars added, and what the next milestone's dev (human or agent) needs to know before touching this code. Append one entry per milestone as it completes — never rewrite past entries, only add to them if a later milestone changes something they assumed.

## Convention: visual theme

Every **frontend** milestone (3, 7, 9, 11, 13, 14) must read every image in `ui-inspirations-theme/` before writing any UI — it's the visual spec (Moniepoint-style Nigerian fintech look: brand-blue backgrounds, white rounded cards, bold headlines, pill buttons, balance-card motifs; see `app/globals.css`'s `--brand`/`--brand-dark`/`--brand-tint` vars from milestone 3 for the established palette). Milestone 3's first pass missed this folder (it isn't referenced from `plan.md`/`architecture.md`) and shipped a generic black/white UI before being redirected — don't repeat that.

## Convention: `integration.md`

After every **frontend** milestone (3, 7, 9, 11, 13, 14), the frontend dev drops an `integration.md` in the project root (overwrite the previous one — it reflects current state, not history; this log is the history) covering:
- Which backend endpoints from [api.md](api.md) it actually calls, and with what shapes.
- Any mismatch found between `api.md`/`api-contracts.md` and what the API really returned, and how it was resolved (fixed the backend, or adapted the frontend — say which).
- Any client-side assumption baked in (e.g. polling vs. realtime, optimistic UI, error states) that a later milestone should know about.
- Manual test notes: what was clicked through in a real browser against the deployed Render URL, per this project's standing rule to test UI changes live before calling them done.

This entry in `handoff.md` should then get a one-line pointer to that milestone's `integration.md` findings, once available.

---

## Milestone 1 — Project scaffold & deploy

- Next.js (App Router, TS) + PWA scaffold, Supabase schema migrated (`supabase/migrations/0001_init.sql`), deployed to Render at `https://proofr.onrender.com`.
- `lib/supabase.ts`: browser (anon key) client + server-only service-role client.
- `/api/health` confirms the live Supabase connection.
- Env vars: see `.env.local.example`. `MONNIFY_*` were left empty — no sandbox credentials configured yet.

## Milestone 2 — Merchant onboarding API

- `POST /api/merchants` and `POST /api/merchants/:id/approve` implemented and verified against the live Render deployment + real Supabase project (rows created/cleaned up in both `auth.users` and `merchants`; RLS scoping confirmed with a real anon-session JWT). Full request/response detail in [api.md](api.md).
- **Mocked**: BVN/NIN verification (`lib/kyc.ts`'s `mockVerifyBvnNin`) — no Monnify sandbox KYC endpoint was reachable with current env vars. See [[monnify-sandbox-only]]: check the sandbox docs before milestone 4/5 assume this needs to stay mocked.
- **New env var**: `ADMIN_API_SECRET` — minimal shared-secret gate for the approve route, not real admin auth (milestone 14 replaces it). Must be set in Render's dashboard and locally in `.env`; not committed anywhere.
- **Seam left for milestone 3**: `POST /api/merchants` accepts an optional `bvnOrNin` field (not in the frozen `api-contracts.md` shape) so the signup UI can pass it inline instead of needing a separate KYC-step endpoint. If milestone 3's UX wants a distinct verification step/screen, the frontend can just call `POST /api/merchants` without `bvnOrNin` first, then decide how to trigger verification later — no second endpoint exists yet for that.
- **Seam left for milestone 4**: the approve route's response always has `monnifyAccountNumber: null`; the hook point to call real Monnify issuance is commented directly above where that variable is set in `app/api/merchants/[id]/approve/route.ts`.

## Milestone 3 — Merchant onboarding UI

- Landing page (`app/page.tsx`), a 4-step signup wizard (`app/signup/page.tsx`: account → BVN/NIN verification → business details → review/submit, all rolled into a single `POST /api/merchants` call), a static pending-approval screen, and a `beforeinstallprompt`-driven install banner (`app/install-prompt.tsx`, wired into `app/layout.tsx`).
- Full detail — endpoint shapes called, a real `api.md` mismatch found (duplicate email returns `422` not the documented `400`, resolved by adapting the frontend to treat any non-2xx generically), client-side assumptions (no polling/realtime, no session persistence, KYC result not distinguishable from the `201` response body yet), and manual browser test notes — in [integration.md](integration.md).
- **Seam left for milestone 7** (revenue dashboard): if that milestone wants to route a merchant from signup straight into an authenticated dashboard, it'll need to add session handling — this milestone deliberately stops at the static pending screen with no session/JWT persisted client-side.

## Milestone 4 — Monnify virtual account issuance

- `lib/monnify.ts`: real Monnify sandbox client — `POST /api/v1/auth/login` (Basic auth, in-process token cache) then `POST /api/v1/bank-transfer/reserved-accounts` (Bearer auth, `contractCode`, deterministic `accountReference: PROOFR-<merchantId>`). Wired into the milestone-2 hook point in `app/api/merchants/[id]/approve/route.ts`. Full request/response detail in [api.md](api.md).
- **Verified against a live Monnify sandbox call, not mocked or code-path-only.** Real `MONNIFY_API_KEY`/`MONNIFY_SECRET_KEY`/`MONNIFY_CONTRACT_CODE` were provided by the user directly into local `.env` (never committed — `.env` stays gitignored; only `.env.local.example` with blank placeholders is tracked). Tested locally end-to-end: signed up a fresh test merchant via `POST /api/merchants`, approved it, got back a real `monnifyAccountNumber` (`4119733541`) and confirmed both `monnify_account_number` and `monnify_account_reference` (`7537FELYM895Z9EX856G`) persisted on the Supabase `merchants` row. Approved the same merchant two more times and got the identical cached account number back both times, with no new Monnify account created — idempotency confirmed.
- **Live-sandbox discrepancy found and fixed**: Monnify's docs (Confluence + developer docs) describe the create-reserved-account response nesting the issued account under a `responseBody.accounts[]` array. The actual sandbox response (without passing `getAllAvailableBanks: true`) returns `accountNumber`/`bankName`/`bankCode` flat on `responseBody` instead. `lib/monnify.ts` handles both shapes defensively, but only the flat shape has actually been observed live.
- **BVN/NIN**: not sent to Monnify. PROOFR never stores raw BVN/NIN (milestone 2's `mockVerifyBvnNin` only persists a verified boolean + hashed reference, see [[monnify-sandbox-only]]), and Monnify's V1 reserved-account endpoint documents `customerBvn` as optional at creation time (only required later, before a *regulated-category* merchant can receive payments) — so this was a non-blocking gap, not a scope violation. Revisit if a later milestone needs `customerBvn` set (would require milestone 2's KYC mock to start persisting the raw value, which it currently does not).
- **Also verified against the live Render deployment** (`https://proofr.onrender.com`, after pushing to `main`): signed up a fresh merchant, approved it, got back a real `monnifyAccountNumber` (`4002101790`), confirmed it persisted on the Supabase row alongside `monnify_account_reference` (`7ZGNEXE3U3MCKETFF11E`), and confirmed re-approving returned the identical cached number (no duplicate account). `MONNIFY_*` env vars were already set in Render's dashboard prior to this milestone. Note: right after `git push`, the first live approve call still returned the old pre-M4 `monnifyAccountNumber: null` shape — Render was still serving the previous build even though `/api/health` was already returning 200 (a still-running old instance stays healthy during a new build). Waited for the new build to finish before re-testing; worth remembering for milestone 5's webhook verification too.
- **Partial-failure behavior**: approval and account issuance are one API call per `api-contracts.md`. If Monnify's call fails, `approval_status` still flips to `approved` (not rolled back), and the response is `{ approvalStatus: "approved", monnifyAccountNumber: null, monnifyError: "<detail>" }` — a 200, not a generic 500, so the caller can see approval succeeded and retry issuance via a second `POST .../approve` call (idempotency check lets that retry through since `monnify_account_number` is still null on the row).
- **No new env vars beyond what was already scaffolded**: `MONNIFY_API_KEY`, `MONNIFY_SECRET_KEY`, `MONNIFY_CONTRACT_CODE` (all already listed in `.env.local.example` since milestone 1, now populated locally with real sandbox values). `MONNIFY_WEBHOOK_SECRET` remains unused — that's milestone 5.
- **Seam left for milestone 5** (webhook ingestion): incoming Monnify transaction webhooks will need to match a payment to a merchant. `monnify_account_number` and `monnify_account_reference` are now populated on approved merchants' rows for exactly that lookup — join/match on whichever of the two Monnify's webhook payload actually includes (check the payload shape when building milestone 5; not confirmed here).

## Milestone 5 — Webhook ingestion

- `POST /api/webhooks/monnify` implemented in [app/api/webhooks/monnify/route.ts](app/api/webhooks/monnify/route.ts); signature verification (`verifyWebhookSignature`) added to [lib/monnify.ts](lib/monnify.ts). Full request/response/field detail in [api.md](api.md).
- **Correction to architecture.md/`.env.local.example`'s milestone-1 assumption**: Monnify does not have a separate, dashboard-configurable "webhook secret." Webhooks are signed with the same `MONNIFY_SECRET_KEY` used for API auth — `HMAC-SHA512(MONNIFY_SECRET_KEY, rawRequestBody)`, hex-encoded, sent in the `monnify-signature` header. Confirmed both from Monnify's docs (Confluence "Computing Request Validation Hash") and against a real live webhook call. `MONNIFY_WEBHOOK_SECRET` is left in `.env.local.example` as an unused no-op placeholder (in case a future Monnify account tier does split it out) rather than deleted; `architecture.md`'s env var table updated to note this. **No new env var was needed** — `verifyWebhookSignature` reads `MONNIFY_SECRET_KEY`, already configured in Render and locally since milestone 4.
- **Verified against a real live Monnify sandbox payment, not just code-path.** Approved a real test merchant ("suya joint", id `27608236-61e7-4a96-afdc-e2d3d872af5c`) to get a real reserved account (`4003488430`, Wema Bank, via `lib/monnify.ts`). Configured Monnify's "Transaction completion" webhook (of the several webhook types their dashboard offers — Refund/Disbursement/Settlement/Mandate/Wallet/Low-Balance/Bills were left unset, out of scope) to `https://proofr.onrender.com/api/webhooks/monnify`. Sent a ₦30,000 transfer via Monnify's Bank Simulator (`https://websim.sdk.monnify.com/?#/bankingapp`) and confirmed a correctly-shaped row landed in the live Supabase `transactions` table within ~2 seconds (`monnify_reference: "MNFY|79|20260719222030|000096"`, `payer_name: "Monnify Limited"`). No docs-vs-live payload discrepancy was found this time (unlike milestone 4's `accounts[]` surprise) — the real payload matched Monnify's documented `SUCCESSFUL_TRANSACTION`/`RESERVED_ACCOUNT` shape.
- **Merchant matching**: done via `eventData.destinationAccountInformation.accountNumber` (the flat bank account number actually paid into) against `merchants.monnify_account_number` — not `product.reference`, which carries the `PROOFR-<merchantId>` accountReference we sent at creation time but never separately persisted on the `merchants` row. If a webhook's account number doesn't match any merchant, the event is acked `200` (Monnify retries won't fix a genuine data mismatch) but logged via `console.error` server-side — a deliberate choice, not a silent drop; check Render logs if transactions seem to be going missing.
- **Idempotency**: relies on the Postgres unique constraint on `transactions.monnify_reference` (from `data-model.md`/`0001_init.sql`) — insert, and on a `23505` unique-violation error, return `200 { ok: true, alreadyProcessed: true }` instead of erroring. Verified live: replayed the real captured webhook payload (freshly signed) against the deployed endpoint and got back the same transaction `id` with no duplicate row.
- **Response timing**: no fraud/revenue engine calls added (neither exists yet — milestones 6 and 8). The route does exactly one Supabase lookup (merchant match) and one insert before acking; no other synchronous work in the critical path.
- **Deploy note confirmed again** (per milestone 4's caution): right after `git push`, the webhook route 404'd for ~60–90s while Render was still serving the old build, even though a plain health check would have looked fine. Polled `POST /api/webhooks/monnify` itself (404 → 401 transition) rather than `/api/health` to know when the new build was actually live, before doing the real sandbox test.
- **Seam left for milestone 6** (revenue engine): `transactions` now has real rows to aggregate over. Useful for milestone 6: `merchant_id` is the join key for per-merchant aggregates; `amount` is numeric (Monnify's `amountPaid`, gross — not `settlementAmount`, which is net of Monnify's fee and currently only lives inside `raw_payload`, not a first-class column — decide there whether "verified revenue" should use gross `amount` or the fee-adjusted `settlementAmount` buried in `raw_payload->'eventData'->>'settlementAmount'`). No index exists yet on `transactions.merchant_id` or `created_at` beyond the PK/unique constraint on `monnify_reference` — worth adding if milestone 6's aggregate queries are slow against real volume.

## Milestone 6 — Revenue engine

- `GET /api/merchants/:id/revenue` implemented in [app/api/merchants/[id]/revenue/route.ts](app/api/merchants/[id]/revenue/route.ts). Full request/response/auth detail in [api.md](api.md).
- **"Gross inflow" vs "verified revenue" definition** (PRD names both, doesn't define the gap): for this milestone they are **identical** — both are `sum(transactions.amount)` for the merchant. Reasoning: "verified" is meant to mean "with fraud-flagged activity excluded," but the fraud rule engine (milestone 8) doesn't exist yet and `fraud_flags` is empty, so there is nothing to exclude today. Making `verifiedRevenue` a different number now (e.g. some ad-hoc discount) would just be an arbitrary number, not a real verification signal. **Milestone 8 must revisit this**: once fraud rules populate `fraud_flags`, `verifiedRevenue` should become `sum(amount) for transactions with no open high/medium-severity flag` (exact threshold is milestone 8's call, see `fraud-rules.md`) while `grossInflow` stays the unfiltered sum — that's the real distinction the PRD is gesturing at.
- **Gross vs net `amount` decision** (seam flagged by milestone 5): both `grossInflow` and `verifiedRevenue` use the first-class `transactions.amount` column (Monnify's gross `amountPaid`), not the fee-adjusted `settlementAmount` buried in `raw_payload->'eventData'->>'settlementAmount'`. Reasoning: `amount` is indexed, typed, and always present; `settlementAmount` is an unindexed JSON path that would need a migration to promote to a first-class column to aggregate cheaply and correctly, and Monnify's fee is a processing-cost detail, not a fraud/authenticity signal — conflating "fee-adjusted" with "verified" would misuse the word the PRD is careful to use for fraud screening. If a later milestone wants a net/settlement figure (e.g. "what actually lands in the merchant's bank"), it should be a third, separately-named field, not a redefinition of `verifiedRevenue`.
- **Auth implemented**: `Authorization: Bearer <supabase-access-token>` — a real Supabase Auth JWT (not a bespoke scheme). The route calls `supabase.auth.getUser(token)` against the anon client to validate it, then authorizes the resulting user as either (a) the owning merchant (`merchant.auth_user_id === user.id`) or (b) any lender (a row in `lenders` with `auth_user_id === user.id`). This was the simplest correct option given what actually exists: milestone 3's signup UI persists no client-side session (per its handoff seam) and milestone 12 (lender portal + its auth) doesn't exist yet, so there was no session/JWT plumbing to reuse and building a parallel bespoke auth layer would just be thrown away by milestone 12. The aggregate query itself runs on the **service-role client**, bypassing RLS — `transactions`' RLS policy (`data-model.md`) only has an owning-merchant `select` policy, no lender policy, so RLS alone can't authorize a lender's read of someone else's transactions; the route's bearer-token check above is what actually gates access, RLS is not relied on here. **Flag for milestone 12**: once lenders get a real portal, this route's "any lender can view any merchant's revenue" check matches `data-model.md`'s stated RLS intent for `merchants`/`reports` ("lenders can select any merchant/report row — search is core to the product"), so no narrowing is expected to be needed, but confirm that's still the intent before this milestone's auth check is reused/extended.
- **New index**: `idx_transactions_merchant_created` on `transactions (merchant_id, created_at)` in [supabase/migrations/0002_revenue_indexes.sql](supabase/migrations/0002_revenue_indexes.sql), covering exactly the filter (`merchant_id`) and sort/bucket (`created_at`) the route's query does. **Applied to the live Supabase project** — the agent's sandboxed environment could only reach the network over HTTPS (raw TCP to port 5432 was blocked, so `psql` couldn't reach `db.<project>.supabase.co` directly), so the user ran this migration file manually against the live database after it was written.
- **Computation approach**: plain in-route aggregation, not a SQL view or scheduled function. The route does one indexed `select(amount, created_at)` query per request and sums/buckets in JS. Given the 2-day timeline and current data volume (single digits to low hundreds of transactions per merchant), this is simpler to get right and to change later than maintaining SQL views that would need to stay in sync with any future column changes; revisit only if a merchant's transaction count grows large enough that summing in JS becomes the bottleneck (not the case today).
- **Trend bucketing**: `trend: [{ period, amount }]`, `period` = `YYYY-MM-DD` by default, or `YYYY-MM` via `?granularity=monthly` (additive query param, not in the frozen `api-contracts.md` shape — omit it for the documented default). Milestone 7's chart can request whichever granularity it needs.
- **Verified against real data**: live-tested locally (Next dev server + real Supabase project, not a local/mocked DB) against the real "suya joint" merchant (`27608236-61e7-4a96-afdc-e2d3d872af5c`) and its real milestone-5 sandbox transaction (₦30,000 from a live Monnify Bank Simulator transfer) — `grossInflow`/`verifiedRevenue` both returned `30000` correctly. Seeded three additional `transactions` rows directly (clearly marked: `monnify_reference` prefixed `TEST-M6-SEED-`, `raw_payload: {"test": true}`) spanning two different months to exercise daily and monthly trend bucketing, confirmed both granularities summed correctly (`54000` gross across 4 rows, correct per-day and per-month buckets), then deleted all three seeded rows and reconfirmed `grossInflow` returned to `30000`. Also confirmed `401` for missing/garbage bearer tokens and `403` for a valid token belonging to an unrelated merchant. Also re-verified against the live Render deployment after pushing — see below for the transcript.
- **Seam left for milestone 7** (revenue dashboard): call `GET /api/merchants/:id/revenue` with the merchant's own Supabase session access token (obtained via `supabase.auth.signInWithPassword` once milestone 3/7 wires up a real signup→session flow — currently no client-side session exists, per milestone 3's seam) to render the totals/trend chart. `?granularity=monthly` is available if the chart wants a coarser view than daily.
- **Seam left for milestone 8** (fraud rule engine): once `fraud_flags` has real rows, come back to this route and change `verifiedRevenue`'s computation to exclude flagged transactions per the definition above — everything else in this route (auth, gross inflow, trend) should be unaffected.

## Milestone 7 — Merchant revenue dashboard

- Closed milestone 3's session gap with `app/login/page.tsx` (`supabase.auth.signInWithPassword`, SDK-default `localStorage` persistence) and a shared browser client singleton (`getBrowserSupabaseClient()` in `lib/supabase.ts`). Built `app/dashboard/page.tsx`: direct RLS-scoped `merchants` read (no new `GET /api/merchants/:id` route — `data-model.md`'s `merchants_select_own` policy already covers it), pending-state reuse of milestone 3's copy, virtual account display with copy-to-clipboard, `GET /api/merchants/:id/revenue` totals + a custom bar trend chart (`app/dashboard/trend-chart.tsx`, no charting library added), and a Supabase Realtime `postgres_changes` subscription on `transactions` filtered to the merchant's own rows. Full detail in [integration.md](integration.md).
- **Found a real gap, since fixed and confirmed live**: Supabase Realtime's `postgres_changes` requires the target table to be added to the `supabase_realtime` publication — `transactions` never was (nothing subscribed to it before this milestone). Added `supabase/migrations/0003_realtime_transactions.sql` (`alter publication supabase_realtime add table transactions;`). This agent's environment couldn't apply it directly (DNS resolution for the raw Postgres host fails in this sandbox, even though HTTPS to the same project works fine — an outbound-network restriction, not a credentials issue; same class of limitation as milestone 6's `0002_revenue_indexes.sql`, and the `supabase` CLI present here is authenticated to an unrelated project so `db push` wasn't an option either). **The user applied the migration manually. Re-verified afterward with a fresh live-fire test: it works** — a test transaction inserted while the dashboard was open triggered the "New payment" badge at t+850ms and the revenue totals updated to the correct amount in the DOM at t+1466ms, with no manual refresh and no console errors. Full before/after transcript in `integration.md`.
- **Everything was verified working end-to-end, against real Monnify sandbox approval**: signup → `POST /api/merchants/:id/approve` (real `ADMIN_API_SECRET`, real Monnify sandbox account issuance) → login → dashboard showing the real virtual account number, session persistence across a reload, sign-out, correct revenue totals/trend once a transaction exists, and now confirmed live realtime updates too. All via Playwright against the live Supabase + Monnify sandbox (Next dev server run locally in this environment; not re-run against the deployed Render URL directly, though it hits the same live backends).
- **One flaky first attempt worth knowing about**: the very first live-fire realtime test (right after the dashboard first loaded and subscribed) timed out waiting for the update — no error, just no event within 20s. A retry a moment later succeeded immediately (850ms). Read as a one-off Realtime channel-join delay, not a defect (not reproduced since); worth keeping in mind for the milestone 16 investor demo — don't fire the demo payment in the same instant the dashboard first renders.
- **Seam left for milestone 9** (fraud flags on dashboard): the revenue card already renders `grossInflow` and `verifiedRevenue` as two separate lines (not merged), anticipating they'll diverge once fraud flags exist — no UI restructuring should be needed, just a flags list/badge to add.

## Milestone 8 — Fraud rule engine

- `lib/fraud.ts`: four standalone, pure rule functions (`checkCircularTransfers`, `checkSelfFunding`, `checkIdenticalTransfers`, `checkVelocitySpike`) plus a `runFraudChecks` orchestrator that queries the relevant Supabase windows and writes `fraud_flags` rows. Wired into `app/api/webhooks/monnify/route.ts` right after the transaction insert, before acking, per `architecture.md`'s diagram. `GET /api/merchants/:id/revenue` now filters `verifiedRevenue` by open flags. Full request/response/field detail in [api.md](api.md).
- **`payer_account` null handling** (applies to circular_transfer, self_funding, identical_transfers — all group/match by payer): a null `payer_account` (Monnify omitted `paymentSourceInformation`) is excluded from same-payer grouping entirely — it can't trigger or contribute to a same-payer match. This is a deliberate under-detection tradeoff: matching null-to-null across unrelated payers would produce false positives. `velocity_spike` is unaffected since it doesn't group by payer.
- **Self-funding's real limitation**: `checkSelfFunding` compares `payer_account` against a `merchantPersonalAccountNumber` parameter, but `merchants` has no such column — milestone 2's KYC mock only ever stored a verified boolean + hashed `kyc_reference`, never a raw BVN/NIN or personal account number (see milestone 2/4 entries, [[kyc-stretch-goal]]). The webhook route calls `runFraudChecks(supabase, inserted, null)` — always `null` today, so this rule is a correct, documented no-op against real data, not a stub. It's implemented against a real (if currently absent) identity value rather than a fabricated one, so it starts working the moment such a value is captured (e.g. if a future milestone adds a `merchants.personal_account_number` column), with no rule-logic change needed.
- **Flag idempotency**: two layers. (1) `runFraudChecks` fetches existing `fraud_flags` for the transaction and skips any `rule_type` already present before inserting. (2) `supabase/migrations/0004_fraud_flags_indexes.sql` adds a unique index on `(transaction_id, rule_type)` as a DB-level backstop, plus an index on `(status, transaction_id)` for the revenue route's flag lookup — **not yet applied to the live Supabase project** (same raw-Postgres-TCP/DNS limitation as migrations 0002/0003; user needs to apply it manually).
- **`verifiedRevenue` definition landed as**: gross sum minus any transaction with *any* open `fraud_flags` row, regardless of severity — not hardcoded to a high/medium severity list. Since all four rules are high or medium (per `fraud-rules.md`), this is equivalent to milestone 6's proposed "open, high or medium" threshold today, but stays correct without a code change if a future rule is ever added at low severity and deliberately excluded from the filter. Overridden flags (`status: "overridden"`) do not exclude a transaction — confirmed live (see below).
- **Verification — rules 1 (circular_transfer) and 3 (identical_transfers)**: verified via the real webhook path — synthetic but correctly HMAC-signed (`HMAC-SHA512(MONNIFY_SECRET_KEY, rawBody)`) payloads POSTed to the local dev server's `/api/webhooks/monnify` against the real "suya joint" merchant (`27608236-61e7-4a96-afdc-e2d3d872af5c`, account `4003488430`), exercising the actual production code path end-to-end (signature verification → merchant match → insert → fraud engine → flag insert). Trigger cases (3 same-payer txns; 5 identical-amount same-payer txns) produced the expected flag; no-trigger cases (2 same-payer; 4 identical-amount same-payer) did not. `monnify_reference` prefixed `TEST-M8-SEED-*` per the milestone 6 convention; all test transactions and flags deleted afterward, confirmed the merchant's only remaining transaction is the real milestone-5 sandbox payment (₦30,000) and `fraud_flags` is empty again.
- **Verification — rule 4 (velocity_spike)**: first attempted against "suya joint" via the real webhook path with a directly-seeded (backdated `created_at`, bypassing the webhook since it always uses `now()`) 60-row baseline — but rules 1/3's own test transactions from moments earlier were still inside the rolling 1h window and inflated the count, confounding a clean no-trigger case (not a bug — `velocity_spike` correctly counts *all* recent transactions merchant-wide, not per-payer, per `fraud-rules.md`'s SQL sketch). Re-verified cleanly against a disposable fixture merchant (created via `POST /api/merchants`, no cross-contamination) with the same backdated-baseline approach, this time seeding the "current" transaction directly and calling `runFraudChecks` directly rather than through the webhook (the fixture has no real Monnify account, so the webhook's merchant-matching step doesn't apply) — 1 recent transaction against the 60-row baseline did not trigger, a 2nd did (recent count exceeded 3x the baseline hourly average), confirming both directions.
- **Verification — rule 2 (self_funding)**: cannot fire against real data today (see limitation above), so verified two ways: (1) 3 unit-style assertions against the pure `checkSelfFunding` function directly (`npx tsx`, no DB) — triggers on a matching identity, doesn't on a mismatched one, doesn't when the identity is `null` (today's real-world input); (2) against the same disposable fixture merchant, called `runFraudChecks` directly with a hypothetical non-null `merchantPersonalAccountNumber` matching a seeded transaction's `payer_account` — correctly produced a `self_funding` flag, confirming the DB-write path works and is ready the moment real identity data exists.
- **Verification — all four rules, pure-function level**: 13 assertions in a throwaway `npx tsx` script (not committed) covering trigger/no-trigger for each rule plus the null-payer-account and zero-baseline edge cases — all passed.
- **Verification — `verifiedRevenue`**: against the same disposable fixture merchant, seeded transactions totaling ₦65,500 gross, of which ₦4,500 ended up flagged (one `velocity_spike`-flagged ₦1,000 txn, plus the ₦2,500 self-funding-test txn — that fixture also happened to be inside the velocity window and picked up a `velocity_spike` flag itself, hence 4 total flags across ₦4,500 of transactions) — `GET /api/merchants/:id/revenue` correctly returned `grossInflow: 65500, verifiedRevenue: 61000`. Then updated all 4 flags to `status: "overridden"` via a direct Supabase update (no override route exists yet, per scope) and re-queried: `verifiedRevenue` recovered to `65500`, confirming overridden flags don't exclude a transaction. Auth for this call used a real Supabase session (`signInWithPassword` against the fixture merchant's own signup credentials), not a bypass. Fixture merchant, its transactions, flags, and auth user were all deleted afterward.
- **No real secrets committed**: confirmed via `git status` — only `lib/fraud.ts`, the two modified routes, and `supabase/migrations/0004_fraud_flags_indexes.sql` are new/changed; `.env` untouched.
- **Seam left for milestone 9** (fraud flags on dashboard): `fraud_flags` now has a real write path with real rows possible (`rule_type`, `severity`, `status`) scoped to a merchant via `transaction_id → transactions.merchant_id`, matching RLS's `fraud_flags_select_own` policy already in place since milestone 1's schema. No new endpoint was added to *read* flags for the dashboard — milestone 9 can either query `fraud_flags` directly via the RLS-scoped browser client (same pattern milestone 7 used for `merchants`) or add a small API route if it wants server-side joins/formatting.
- **Seam left for milestone 10** (report generation / confidence score): `fraud-rules.md`'s penalty math (-20/distinct payer for circular_transfer, -30 flat for self_funding, -10/group for identical_transfers, -15 flat for velocity_spike, floored at 0, starting from 100) can now run against real `fraud_flags` rows instead of an empty table. Note `circular_transfer` and `identical_transfers` can each produce multiple flags per merchant across different transactions/payers/groups — milestone 10's confidence-score computation needs to decide whether to count distinct *flags* or distinct *triggering payers/groups* per `fraud-rules.md`'s wording ("per distinct triggering payer" / "per triggering group"); this engine writes one flag per rule per transaction, not one flag per group, so milestone 10 may need to dedupe/group flags by payer or amount to match the penalty wording exactly.

## Milestone 9 — Fraud flags surfaced on dashboard

- `app/dashboard/fraud-flags.tsx` (new `FraudFlagsCard` component) + changes to `app/dashboard/page.tsx`: direct RLS-scoped read of `fraud_flags` embedding `transactions` (no new API route — same pattern milestone 7 used for `merchants`), rendered as a severity-badged, plain-language-labeled list on the dashboard. Open flags render as active/concerning; `status: "overridden"` flags render in a separate, visually muted "Cleared" section rather than mixed in. A small red note now appears under revenue totals when `grossInflow > verifiedRevenue` ("₦X excluded due to flagged activity"). Full detail, exact query, and manual test transcript in [integration.md](integration.md).
- **Found and fixed the same class of gap milestone 7 hit**: `fraud_flags` was never added to the `supabase_realtime` publication, so a dedicated realtime subscription on it (added alongside the existing `transactions` subscription, since `runFraudChecks` writes flags moments *after* the transaction insert in the same webhook request — a real race, not hypothetical) never fired. Added `supabase/migrations/0005_realtime_fraud_flags.sql` (`alter publication supabase_realtime add table fraud_flags;`). Same raw-Postgres-DNS limitation as migrations 0002–0004 — **the user applied it manually against the live Supabase project mid-milestone**, and it was re-verified live immediately after: a flag written moments after its transaction now appears in the dashboard DOM within ~1.2s, with no manual refresh, confirmed with a uniquely-marked fixture transaction to rule out a false-positive match.
- **Verified live, not just implemented**: seeded a disposable fixture merchant (`TEST-M9-SEED-*` convention) with a clean transaction, a high-severity open flag (`self_funding`), a medium-severity open flag (`velocity_spike`), and an overridden flag (`identical_transfers`) — all four rendered correctly (correct labels, correct severity colors, overridden flag visually separated), then reproduced the realtime race before the migration was applied (flags didn't live-update; a manual reload confirmed the underlying data/query was correct) and confirmed it resolved after. All test data deleted afterward; confirmed `fraud_flags` empty for that merchant id and `/api/health`'s merchant count back to baseline.
- **No new API route** — per milestone 8's seam note leaving that choice open, a server-side route wasn't needed since PostgREST's embedded-resource `select` already expresses the join in one round trip.
- **Seam left for milestone 10** (report generation): the same `fraud_flags` read pattern (embedding `transactions`) used here could be reused or adapted for assembling `fraud_flags_snapshot` in a generated report, though milestone 10 will likely want raw flag rows rather than the plain-language labels this milestone adds client-side for display.

## Milestone 10 — Proof-of-Revenue report generation

- `POST /api/merchants/:id/report` and `GET /api/merchants/:id/report` implemented in [app/api/merchants/[id]/report/route.ts](app/api/merchants/[id]/report/route.ts). Revenue aggregation factored out of the milestone 6 revenue route into [lib/revenue.ts](lib/revenue.ts) (`computeRevenueSummary`, now shared by both routes with zero duplicated logic) and confidence scoring lives in [lib/confidence.ts](lib/confidence.ts) (`computeConfidenceScore`). Full request/response/auth detail in [api.md](api.md).
- **Resolved milestone 8's flag-grouping seam**: `fraud-rules.md` words penalties per *distinct triggering group* (circular_transfer: per payer, identical_transfers: per payer+amount group), but `lib/fraud.ts` writes one flag row per rule per qualifying transaction, so a merchant with 5 identical-amount transactions from one payer can have up to 5 `identical_transfers` rows for what the spec calls one group. **Chose grouping over per-row summation** — `lib/confidence.ts` dedupes open flags before penalizing: `circular_transfer` keyed by `payer_account`, `identical_transfers` keyed by `payer_account + amount`, and `self_funding`/`velocity_spike` each collapsed to a single flat deduction regardless of how many flag rows exist for that rule (per fraud-rules.md's "single occurrence is enough" wording for self_funding, and velocity_spike having no per-group notion at all — it's a merchant-wide check). This is explicit and documented in `lib/confidence.ts`'s file comment, not left ambiguous in the code. Note this dedup logic assumes `circular_transfer`/`identical_transfers` flags always carry a non-null `payer_account` — true today because `lib/fraud.ts`'s `checkCircularTransfers`/`checkIdenticalTransfers` both bail out on a null `payer_account` before ever flagging, so no null-key fallback grouping was needed.
- **Confidence score**: starts at 100, floors at 0, only `status: "open"` flags count (`overridden` flags contribute nothing to the score and are excluded from `fraud_flags_snapshot` entirely — the snapshot is "what's actively affecting this report," not a full audit trail; the full flag history remains queryable from `fraud_flags` directly if ever needed).
- **Report-sharing auth is a deliberate placeholder, not a real mechanism**: per `api-contracts.md`'s "lender with a valid share link/report ID" and milestone 12 (lender auth) not existing yet, `GET /api/merchants/:id/report?reportId=<uuid>` performs **no auth check** — knowledge of the unguessable v4 UUID is treated as sufficient. Nothing expires it, scopes it to one viewer, or lets a merchant revoke a share after the fact. **What a real implementation would need**: a signed/expiring share token (e.g. an HMAC'd value or JWT with an expiry claim, minted by the merchant at share time) instead of the bare row id, so a leaked link can't be used forever and a merchant can invalidate a report they no longer want shared.
- **`POST`'s auth is merchant-only** (no lender path) per the frozen contract — matches `GET /api/merchants/:id/revenue`'s bearer-token pattern (`supabase.auth.getUser(token)` then check `merchants.auth_user_id`), just without the lender branch since generating a new report snapshot isn't a lender action.
- **`profile`/`verificationStatus` in the `GET` response are read live from `merchants`**, not baked into the stored snapshot — `business_name`/`bvn_nin_verified`/`approval_status` can legitimately change after a report was generated (e.g. a rename, or KYC re-verification), and re-reading is one cheap indexed lookup, not worth denormalizing into `reports`. Everything else in the response (`revenueSummary`, `trendData`, `confidenceScore`, `fraudFlags`) is the stored snapshot as-is — per `data-model.md`, an admin override (milestone 14) will not retroactively change an already-generated report's stored score, by design.
- **Timing measured, not assumed**: against the local dev server hitting the live Supabase project (same pattern milestone 6/8 used for their own timing/correctness checks), `POST` measured **1.56s** (clean merchant, 1 transaction) and **1.67s** (4 transactions + 4 flags) via `curl -w time_total` — comfortably under the PRD's 5s threshold. The route does exactly two queries in parallel (one indexed `transactions` scan reused from `lib/revenue.ts`, one `fraud_flags` join to `transactions` for open flags) plus one insert; none of that scales with report count, only with a single merchant's transaction/flag volume, which stays small for the hackathon's data.
- **Verified against real data, not just code-path**: seeded a disposable `TEST-M10-SEED-*` merchant (created via `POST /api/merchants`, approved via `POST /api/merchants/:id/approve`, session obtained via a direct Supabase Auth password-grant call — same auth users mechanism prior milestones use). Clean case: 1 real transaction, no flags → `confidenceScore: 100`, `grossInflow === verifiedRevenue`. Seeded-flags case: added 2 `identical_transfers` flags on 2 different transactions sharing one payer_account+amount (simulating `lib/fraud.ts`'s one-row-per-transaction behavior for what should be one group), 1 `self_funding` flag, and 1 `overridden` `circular_transfer` flag — confirmed `confidenceScore: 60` (100 − 30 − 10, overridden flag contributing nothing) and `verifiedRevenue` correctly excluding all three open-flagged transactions' amounts. Also confirmed both `GET` auth paths (owning-merchant bearer token for "latest report," and unauthenticated `?reportId=` fetch) return the identical, correct shape. All seeded transactions, flags, reports, the merchant row, and its auth user were deleted afterward; confirmed `/api/health`'s merchant count back to the pre-test baseline (6).
- **No real secrets committed**: confirmed via `git status`/diff — only `lib/revenue.ts`, `lib/confidence.ts`, the new report route, the refactored revenue route, and this doc set changed; `.env` untouched.
- **Seam left for milestone 11** (report view/share UI): `GET /api/merchants/:id/report` is ready to render directly — `fraudFlags` is raw flag rows (`rule_type`/`severity`/`status`/`payer_account`/`amount`/`transaction_id`/`created_at`), not the plain-language labels milestone 9's dashboard component adds client-side, so milestone 11 will want its own display-formatting layer (could reuse/extract milestone 9's label logic from `app/dashboard/fraud-flags.tsx` rather than reinvent it). The share-link placeholder above means milestone 11's "share with a lender" UI should surface the `reportId`-as-URL caveat to the user (or milestone 11 could be where the real signed-token mechanism gets built, if it's in scope).
- **Seam left for milestone 13** (lender portal): `GET /api/lenders/merchants/:id` is specced to return "the same shape as `GET /api/merchants/:id/report`" — this route's bearer-token-or-lender branch already exists and could likely be reused directly (call the same handler logic, or literally alias the route) rather than reimplemented; the report route's lender bearer-token check already exists for exactly this reuse.

## Milestone 11 — Report view/share

- `app/report/[id]/page.tsx` (`[id]` = merchant id) — one route serves both the merchant's own "latest report" view (bearer token, no `?reportId=`) and the unauthenticated share-link view (`?reportId=<uuid>`, zero auth headers sent). `app/dashboard/page.tsx` gained a "Generate report" button (`POST` then navigate straight to `/report/:merchantId?reportId=<new id>`) and a "View my latest report" link. Fraud-flag plain-language labels were extracted from milestone 9's `app/dashboard/fraud-flags.tsx` into `lib/fraud-labels.tsx` so the report's flat flag rows reuse the same `RULE_LABELS`/`SeverityBadge`/formatting instead of a second copy. Download is a `window.print()` button with `print:` Tailwind variants + a `@page` rule in `app/globals.css` (no server-side PDF generation, per this milestone's scope). Full detail in [integration.md](integration.md).
- **Found and fixed a real `api.md` mismatch**: `GET /api/merchants/:id/report`'s response never included the report's own id, so there was no way for the "latest report" bearer-token path to construct a `?reportId=` share URL — the entire point of this milestone. Fixed the backend (`app/api/merchants/[id]/report/route.ts`'s `buildReportResponse` now returns `reportId: report.id`) rather than working around it client-side; both existing queries already selected `id`, so this was a one-line addition, not a new query. `api.md`'s milestone 10 example response block should be read as including `"reportId": "..."` at the top level now.
- **Share-link mechanism is exactly milestone 10's documented placeholder, surfaced honestly to the user**: the report page shows "Anyone with this link can view this report — links don't expire yet, so only share it with people you trust" next to the copy-link button. No signed/expiring token was built (out of scope per milestone 10/11's handoff note) — that remains a real gap if this goes further than the hackathon.
- **Verified against the live Supabase + Monnify sandbox project**: seeded a disposable `TEST-M11-SEED-*` merchant (real Monnify sandbox approval), seeded one clean transaction and one with an open `self_funding` flag plus an overridden `velocity_spike` flag elsewhere, generated a report, and confirmed the bearer-token "latest" fetch and the zero-auth `?reportId=` fetch return **byte-for-byte identical JSON** (`diff`-confirmed) with correct numbers (`confidenceScore: 70`, `verifiedRevenue` excluding the open-flagged transaction). All test data deleted afterward.
- **Gap closed**: pushed to `main` (`d453d85`) and confirmed the Render redeploy picked up the fix — re-seeded a fresh `TEST-M11-SEED-*` merchant (real Monnify sandbox approval) directly against `https://proofr.onrender.com`, generated a report, and the very first `GET` (bearer, latest) already returned `reportId` (no old-build lag this time, unlike milestones 4/5's experience). Re-ran the identical-response check against the **deployed URL itself** (not just locally): bearer-token "latest" fetch and the zero-auth `?reportId=` fetch returned **byte-for-byte identical JSON** (`diff`-confirmed), and both `/report/:id?reportId=...` and `/dashboard` returned `200` live. All test data deleted afterward, confirmed `business_name=like.TEST-M11-SEED*` returns `[]`.
- **Still not done**: **no browser-automation tool was available in this agent's environment**, so the literal click-through (open the dashboard, click "Generate report," click "Copy share link," paste the URL into a fresh incognito window, confirm it renders identically) was verified at the API-contract level against the live deployment (identical JSON, zero auth required, pages return 200) but not by actually driving a browser end-to-end. A real incognito-window pass is recommended before fully closing this milestone's done-when.
- **Seam left for milestone 12/13** (lender portal): the report page's unauthenticated share-link view is already exactly what a lender lands on when they follow a shared link — milestone 12/13 can link straight to `/report/:merchantId?reportId=<uuid>` from lender search results rather than building a separate lender-facing report renderer.

## Milestone 12 — Lender portal API

- Implemented `GET /api/lenders/search`, `GET /api/lenders/merchants/:id`, `POST /api/loans`, `POST /api/loans/:id/approve` in [app/api/lenders/search/route.ts](app/api/lenders/search/route.ts), [app/api/lenders/merchants/[id]/route.ts](app/api/lenders/merchants/[id]/route.ts), [app/api/loans/route.ts](app/api/loans/route.ts), [app/api/loans/[id]/approve/route.ts](app/api/loans/[id]/approve/route.ts). Full request/response detail in [api.md](api.md).
- **Resolved the missing lender-provisioning gap** (flagged by this milestone's own build prompt: nothing in `plan.md`/`api-contracts.md` specs a public lender signup route, and `userflows.md`'s lender flow starts at login, not signup). No `POST /api/lenders` route was built — inventing one risks a shape milestone 13/16 doesn't expect. Instead, one real lender was provisioned directly against the live Supabase project using the same two-step mechanism `POST /api/merchants` uses internally (`auth.admin.createUser` via the Auth Admin REST API, service-role key, `email_confirm: true`, then an insert into `lenders` with the returned `auth_user_id`) — run as a disposable, uncommitted script, not a new endpoint. **Test lender account**: email `lender-demo@proofr.test`, org name "Demo Capital Partners", `lenders.id` `0fabf7ef-8014-43ca-bfd0-3743b8e434cb`. Password was reported to the user out-of-band (not committed anywhere in the repo) — **milestone 16's demo rehearsal needs to ask the user for it**, or re-provision a fresh lender the same way (create a Supabase Auth user + matching `lenders` row) if it's been lost. A lender authenticates identically to a merchant: `supabase.auth.signInWithPassword({ email, password })` against the browser client, then sends the resulting access token as `Authorization: Bearer <token>` — confirmed working end-to-end against every route below.
- **`GET /api/lenders/merchants/:id` reuses, not reimplements**: per `api-contracts.md`'s "same shape as `GET /api/merchants/:id/report`" and milestone 10's handoff seam note, the merchant-report route's entire bearer-token-owns-the-record-or-lender GET logic was extracted into `lib/reports.ts`'s `getLatestReportForBearerToken` (plus `buildReportResponse`). `GET /api/merchants/:id/report` (no `reportId`) and `GET /api/lenders/merchants/:id` both now call this one function — literally the same code path, not a duplicate re-implementation, and auth is unchanged from what milestone 10 already had (owning merchant OR any lender). One side effect: a merchant's own bearer token also succeeds against `GET /api/lenders/merchants/:id` (returns their own report) — this is the existing merchant-or-lender check working as designed, not a new hole opened by this milestone.
- **`confidenceScore` for an unscored merchant**: `GET /api/lenders/search` returns `confidenceScore: null` for a merchant with no `reports` row yet, rather than omitting them from results or defaulting to `100`. Omitting would make the merchant invisible to search (worse for the lender flow); defaulting to `100` would misrepresent "never scored" as "scored perfectly clean." `null` is unambiguous and lets milestone 13's UI render "not yet scored" explicitly. Verified live: a seeded approved merchant with zero transactions/reports came back `confidenceScore: null` in search results.
- **Search implementation**: two separate parameterized Supabase queries (`ilike` on `business_name`, plus an `eq` on `id` only when `query` matches a UUID regex) merged in JS by merchant id, not a single PostgREST `.or()` filter string — avoids a query containing commas/parens being interpreted as extra filter syntax. For merchants with a report, uses each merchant's single most-recently-generated report's `confidence_score` (one `in(...)` query across all matched merchant ids, ordered by `generated_at desc`, first row per merchant id kept) — one extra query total, not N+1, acceptable at hackathon result-set sizes.
- **`mockRepaymentSchedule` is a deliberate placeholder**, the same way milestone 2 left `monnifyAccountNumber: null` for milestone 4 to fill in for real: `POST /api/loans/:id/approve` splits the loan amount evenly across 3 fixed monthly periods with no interest/fees modeled, computed synchronously at approval time and stored in `loans.mock_repayment_schedule`. Milestone 15 (repayment automation) owns the real "simulated deduction from future revenue" logic and is expected to **replace** this computation, not extend it.
- **Loan auth**: `POST /api/loans` uses the new lender-only auth helper (`lib/lender-auth.ts`'s `authenticateAsLender` — same bearer-token pattern as everywhere else, scoped to a `lenders` row instead of merchant-or-lender) and sets `lender_id` from the authenticated lender, never from the request body. `POST /api/loans/:id/approve` additionally checks `loan.lender_id === lender.id` before approving — per `data-model.md`'s RLS intent that a lender only touches their own `loans` rows — confirmed live that a different authenticated lender (or a merchant's own token, which isn't a lender at all) gets `403` on someone else's loan.
- **Verified against real, live data — locally against the live Supabase project first, per milestone 10's pattern**: seeded a disposable `TEST-M12-SEED-*` merchant (real signup + admin approval), one directly-inserted `TEST-M12-SEED-*` transaction (₦42,000, no fraud flags), generated a report as that merchant (`confidenceScore: 100`), then as the real test lender: `GET /api/lenders/search?query=TEST-M12-SEED` and `?query=<merchantId>` both returned the merchant with `confidenceScore: 100`; `GET /api/lenders/merchants/:id` returned a response byte-identical in shape to `GET /api/merchants/:id/report`'s bearer-token path (`grossInflow`/`verifiedRevenue: 42000`, `confidenceScore: 100`); `POST /api/loans` created a `pending` loan; `POST /api/loans/:id/approve` flipped it to `approved` with a 3-period even-split `mockRepaymentSchedule` (₦30,000 × 3). A second merchant with no report confirmed the `confidenceScore: null` path. Auth-failure matrix confirmed on all four routes: `401` with no token, `403` with a merchant's own token on lender-only routes (search, loans), and `403` approving a loan that belongs to a different lender. All seeded merchants, transactions, reports, the loan, and both merchants' auth users were deleted afterward; `/api/health`'s merchant count confirmed back to baseline (6). Then pushed to `main` and re-ran the identical seed/search/detail/loan/approve/cleanup flow **against the live `https://proofr.onrender.com` deployment** with a fresh `TEST-M12-SEED-*` merchant — same results, confirming the Render build picked up the change.
- **No real secrets committed**: confirmed via `git status`/diff — only the new lender/loan routes, `lib/reports.ts`, `lib/lender-auth.ts`, the refactored merchant report route, and this doc set changed; `.env` untouched; the test lender's password lives only in this handoff entry's out-of-band note to the user, not in any tracked file.
- **Seam left for milestone 13** (lender portal UI): all four routes are ready to build against as-is. The lender login screen just needs `supabase.auth.signInWithPassword` with the test lender's credentials (see above) to get a working session — no new auth UI pattern needed beyond what milestone 3's merchant login already established. Search results' `confidenceScore: null` case needs an explicit "not yet scored" UI state, not a blank/zero render. Loan approval's `mockRepaymentSchedule` is real data (3 periods, even split) and safe to render as-is, but milestone 13 should avoid presenting it as anything more precise than "a rough placeholder schedule" given milestone 15 will replace the underlying math.

## Milestone 13 — Lender portal UI

- `app/login/page.tsx` extended (not duplicated) to branch after sign-in: checks for a `merchants` row first, then a `lenders` row, routes to `/dashboard` or `/lender` accordingly. New `app/lender/page.tsx` (search) and `app/lender/merchants/[id]/page.tsx` (summary + mock loan approval, linking straight to milestone 11's existing `/report/:id?reportId=` view rather than a second report renderer). Full endpoint/shape detail, client-side assumptions, and manual test transcript in [integration.md](integration.md).
- **No mismatch found** between `api.md`'s milestone 12 section and what the live routes actually returned — all four lender/loan routes matched their documented shapes exactly on the first try, including the `confidenceScore: null` case.
- **Client-side lender gating, on top of existing backend auth**: `GET /api/lenders/merchants/:id` is intentionally merchant-or-lender server-side (milestone 12's design), so both new pages check for a `lenders` row on load and redirect a non-lender session (including a merchant's own) straight to `/dashboard` before rendering any search/loan UI — confirmed live that a merchant session bounces off `/lender` even when navigated to directly.
- **Loan approval is a single button, not a two-step pending/approve UI** — calls `POST /api/loans` then `POST /api/loans/:id/approve` in sequence and renders the resulting `mockRepaymentSchedule` under an "Estimated repayment schedule" heading with an explicit rough-placeholder caption, per this milestone's own handoff note above.
- **Verified with a real, live-driven browser flow (Playwright), not just implemented**: seeded two disposable merchants (`TEST-M13-SEED Scored Merchant` with a real report/₦42,000 verified revenue/`confidenceScore: 100`, `TEST-M13-SEED Unscored Merchant` with none) against the live Supabase project. Signed in as the real test lender, searched, confirmed both the numeric score badge and the "Not yet scored" badge render correctly, viewed both merchants' summaries, followed the scored merchant's report link through to a correctly-rendering full report page, and approved a real mock loan on the unscored merchant end-to-end (real `POST /api/loans` + `POST /api/loans/:id/approve` calls, real repayment schedule rendered). Also confirmed a merchant's own session cannot reach `/lender`. Then pushed to `main` (`9e000c5`) and, once Render's redeploy landed, re-ran the identical Playwright flow **against `https://proofr.onrender.com` itself** (same seed accounts, no reseeding needed) — identical results, including a second real mock loan approval live on Render. All seed data (both merchants, the transaction, the report, both loans, and both seed accounts' Supabase Auth users) deleted afterward; confirmed `/api/health`'s merchant count back to baseline (6) and the real test lender/other pre-existing auth users untouched. Full transcript in `integration.md`.
- **Test lender's password**: the user provided it directly, out-of-band, for this milestone's manual testing only — it was used interactively and never written to any file in the repo or in this doc.

## Milestone 14 — Admin stub

- `app/api/admin/fraud-queue/route.ts` (`GET`) and `app/api/admin/fraud-flags/[id]/override/route.ts` (`POST`), plus `app/admin/page.tsx`. Full request/response detail, the admin-auth decision, the clear/confirm schema resolution, and manual test notes in [integration.md](integration.md).
- **Admin-auth decision, stated plainly so it isn't mistaken for a gap later**: milestone 2's note that "milestone 14 replaces" `ADMIN_API_SECRET` does **not** mean a real admin identity/login system was built. Nothing in `data-model.md` (no `admins` table), `api-contracts.md` (no admin signup/login route), or `architecture.md` (admin access is explicitly service-role-key-backed server routes, not a Supabase Auth role) supports one — inventing one would repeat the exact mistake milestone 12 deliberately avoided for lender signup. `ADMIN_API_SECRET` remains the real security boundary; `app/admin/page.tsx` just gives it a UI (a `sessionStorage`-only secret prompt, re-prompting on `401`, no email/password/"remember me"). `fraud_flags.reviewed_by` is left `null` on every override — no real admin auth user exists under this scheme to attribute it to.
- **Clear/confirm resolved as two different effects on one two-valued `status` column**: `"clear"` → `status: "overridden"` (the action that actually changes `verifiedRevenue`/confidence score, since `lib/revenue.ts` and `lib/confidence.ts` both only look at `status: "open"`), `"confirm"` → `status` stays `"open"` but `reviewed_at` is set (admin agrees it's real fraud, no scoring change, just marks it reviewed). `"confirm"`'s response is `{ flagId, status: "open" }` — the frozen contract's own example only shows the `overridden` case and doesn't spell out `confirm`'s shape; this is the honest shape given what actually happens server-side.
- **`GET /api/admin/fraud-queue` extends the frozen response** with `amount`, `payerName`, `businessName` (joined from `transactions`/`merchants` server-side) beyond the contract's bare `flagId/transactionId/merchantId/ruleType/severity/createdAt` shape — needed so a human admin has enough context to make a clear/confirm call without a second round-trip per row. Only **open** flags are listed (a queue is unresolved work); a `confirm`ed-but-still-open flag stays in the list, distinguished only client-side (a "Confirmed — still open" badge, from local component state, not a server-side field).
- **Verified the cross-milestone effect the done-when criterion actually asks for** (not just the override route's own `200`): seeded a real `TEST-M14-SEED-*` transaction + open fraud flag against a real merchant, drove the full flow through a real Playwright browser session against `app/admin/page.tsx` — wrong secret correctly re-prompts, correct secret loads the real open queue, clicking Clear removes the row from the UI within ~1s — then **recomputed the merchant's `verifiedRevenue` directly using the same aggregation `lib/revenue.ts`'s `GET /api/merchants/:id/revenue` uses**, confirming the cleared transaction's amount now counts toward verified revenue. A second scripted pass confirmed `"confirm"` leaves `verifiedRevenue` unchanged (flag stays open) while still setting `reviewed_at`. Full auth/error matrix confirmed: `401` (wrong/missing secret), `400` (invalid `action`), `404` (unknown flag id). All seeded data cleaned up afterward. Ran first against a local `next dev` server pointed at the live Supabase project, then re-ran the identical scripted flow **against the deployed `https://proofr.onrender.com`** after pushing to `main` — see the confirmation note below.
- **No real secrets committed**: `.env` untouched; `ADMIN_API_SECRET` only ever read server-side from `process.env`, never embedded in any tracked file — the admin page only holds whatever string a human types into the `sessionStorage`-backed prompt at runtime.
- **Confirmed live on Render** (`https://proofr.onrender.com`) after pushing to `main` (`507f03b`): polled `/api/admin/fraud-queue` until the new build was live (5 attempts, ~100s), then re-ran the identical scripted seed/queue/confirm/clear/cleanup flow against the live deployment with a fresh `TEST-M14-SEED-RENDER-*` transaction (₦4,200, `identical_transfers`) on the same test merchant (`suya joint`) — identical results to the local run: flag appeared in the live queue with joined `businessName`/`amount`/`payerName`; `confirm` left `verifiedRevenue` at `30000` (unchanged) while setting `reviewed_at`; `clear` moved it to `34200` (the ₦4,200 now counted); the full `401`/`400`/`404` error matrix matched. Seeded data deleted afterward; recomputed revenue matched the pre-seed baseline (`30000`) exactly. `https://proofr.onrender.com/admin` also confirmed reachable (`200`).

## Milestone 15 — Repayment automation illustration

- `lib/repayment.ts`'s `applyRepaymentDeductions`, wired into `app/api/webhooks/monnify/route.ts` right after milestone 8's fraud engine call, before acking. `POST /api/loans/:id/approve` (`app/api/loans/[id]/approve/route.ts`) keeps milestone 12's schedule *shape* (3 even monthly periods, no interest) but each period now starts with progress fields (`status: "pending"`, `paidAmount: 0`, `paidAt: null`) for this milestone's deduction logic to advance. New `GET /api/loans/:id` (`app/api/loans/[id]/route.ts`) added to observe progress. Full request/response shapes and the mechanism's exact wording in [api.md](api.md).
- **Deduction mechanism chosen: per-transaction waterfall, not a percentage-of-transaction or accumulate-then-trigger scheme.** On each new transaction, look up the paying merchant's `approved`/`repaying` loans and apply the transaction's full amount against the schedule's oldest unpaid period first; any remainder cascades into the next period, and so on. **Why this over the alternatives plan.md's build prompt suggested**: a flat percentage-of-each-transaction deduction would still be "tied to revenue" but would take many small transactions to ever complete a period, poorly suited to demoing a full loan lifecycle in a live investor demo (milestone 16). The "accumulate since last update, mark paid when the accumulation covers a period" scheme the build prompt sketched needs to track a running accumulator separate from `paidAmount` for the current (not-yet-fully-paid) period; a waterfall achieves the identical effect without that separate field, since `paidAmount` already **is** the running accumulator for whichever period is currently open, and a single large transaction can complete multiple periods in one webhook call (demonstrated live below) — genuinely tied to the transaction's real amount, not a fixed schedule, exactly per userflows.md's "against future revenue" wording.
- **Explicitly does not touch `transactions`, `fraud_flags`, `revenue_summary`/`verifiedRevenue`/`grossInflow` computations** (`lib/revenue.ts`, `lib/confidence.ts`) — confirmed live below: `grossInflow` after 3 test transactions equaled their exact sum, and `verifiedRevenue`'s reduction below that sum was solely the pre-existing fraud engine (milestone 8) flagging same-payer/rapid-succession test transactions — a real, expected fraud-engine effect, unrelated to and unperturbed by the loan's repayment progress. A later milestone must not assume loan repayment silently reduces reported revenue anywhere in the app; it doesn't.
- **`GET /api/loans/:id`**: lender-only auth (`lib/lender-auth.ts`'s `authenticateAsLender`), scoped to the loan's own lender — identical ownership check to `POST /api/loans/:id/approve`. Returns `{ loanId, status, mockRepaymentSchedule }`, the schedule now carrying live per-period `status`/`paidAmount`/`paidAt`. No merchant-facing loan route was built (`data-model.md` has no RLS policy letting a merchant read their own `loans` rows, and this milestone is tagged `(Backend)`-only with no dedicated frontend milestone owning it per `plan.md`) — a reasonable minimal addition for a future milestone if userflows.md's merchant-side step 13 needs independent API verification, but out of this milestone's scope.
- **Verified against the live Render deployment, real webhook path, not just code-path**: seeded a disposable `TEST-M15-SEED-*` merchant (real signup + admin approval, real Monnify sandbox account issuance) and a disposable `TEST-M15-SEED-*` lender (same direct-provisioning mechanism milestone 12 used, cleaned up after — not the persistent test lender account). Created and approved a real ₦30,000 mock loan (3 × ₦10,000 periods) via `POST /api/loans` + `POST /api/loans/:id/approve`. Sent three real, correctly HMAC-signed (`HMAC-SHA512(MONNIFY_SECRET_KEY, rawBody)`) webhook payloads to `POST /api/webhooks/monnify` against the merchant's real reserved account number — **this is the honest path used**: real signed webhook calls, not a direct database write or a direct call into `lib/repayment.ts` bypassing the route. Progression observed via `GET /api/loans/:id` after each:
  - ₦4,000 → loan `status: "repaying"`, period 1 `paidAmount: 4000`, still `"pending"`.
  - ₦9,000 → period 1 completes (`paidAmount: 10000`, `"paid"`, `paidAt` set), ₦3,000 remainder flows into period 2 (`paidAmount: 3000`, still `"pending"`); loan stays `"repaying"`.
  - ₦20,000 → period 2 completes (needs ₦7,000 more) and period 3 completes in the same call (₦10,000 more, exact — no overpay remainder to observe in this run); loan `status: "repaid"`, all three periods `"paid"`.
  - Confirmed the full lifecycle reaches `repaid`, not just the first deduction, per the done-when's explicit requirement.
- **Revenue numbers confirmed untouched**: `GET /api/merchants/:id/revenue` before any transactions returned `{ grossInflow: 0, verifiedRevenue: 0 }`; after all three (₦4,000 + ₦9,000 + ₦20,000 = ₦33,000) returned `{ grossInflow: 33000, verifiedRevenue: 13000 }` — the ₦20,000 gap is milestone 8's fraud engine correctly flagging the three same-payer, rapid-succession test transactions (expected, unrelated to this milestone), and `grossInflow` exactly equals the transactions' sum, confirming the revenue aggregation path is entirely independent of loan status/schedule.
- **Cleanup confirmed**: the seeded loan, transactions (and any fraud flags on them), lender (`lenders` row + its `auth.users` row), and merchant (`merchants` row + its `auth.users` row) were all deleted after the run. No real API keys or secrets were added or committed — `git status`/diff confirms only `lib/repayment.ts` (new), `app/api/loans/[id]/route.ts` (new), and the two modified routes (`app/api/loans/[id]/approve/route.ts`, `app/api/webhooks/monnify/route.ts`) plus this doc set changed; `.env` untouched.
- **Seam left for milestone 16** (demo rehearsal): to visibly trigger a repayment live during the investor demo, approve a mock loan for the demo merchant via the milestone 13 lender portal UI, then send (or replay) a Monnify sandbox transfer to that merchant's reserved account — the existing webhook path (no new demo-only code) will automatically apply the deduction and advance the schedule. `GET /api/loans/:id` (lender-only) is the fetch to poll/call to show the schedule updating live, since no UI renders it yet — milestone 16 could add a quick lender-portal card for this, or just narrate the API response, depending on how much UI time remains.
